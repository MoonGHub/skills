interface:
  display_name: "Project AI Context Builder"
  short_description: "Build portable AI_CONTEXT and AGENTS guides"
  default_prompt: "Use $project-ai-context-builder to create or refresh portable, source-grounded AI_CONTEXT and AGENTS.md so a new session or machine can continue with targeted reading instead of a full repository re-analysis."
